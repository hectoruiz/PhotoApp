ext {

    // Plugins
    gradlePluginVersion = "7.0.3"
    kotlinPluginVersion = "1.5.31"

    // App SDK Properties
    compileSdkVersion = 31
    minSdkVersion = 21
    // Manifest merge issue with targetSdk 31
    targetSdkVersion = 30
    versionCodeVersion = 1
    versionNameVersion = "1.0"

    // Java 8 Compatibility
    javaVersion = JavaVersion.VERSION_1_8
    jvmTargetVersion = "1.8"

    // Android Libraries
    coreKtxVersion = "1.7.0"
    appCompatVersion = "1.3.1"
    activityVersion = "1.4.0"
    materialVersion = "1.4.0"
    constraintLayoutVersion = "2.1.1"
    lifecycleVersion = "2.4.0"
    fragmentExtensionsVersion = "1.3.6"
    roomVersion = "2.3.0"
    toolsAndroidVersion = "1.1.5"
    kotlinCompilerExtensionVersion = "1.0.5"
    activityComposeVersion = "1.0.5"
    composeVersion = "1.4.0"
    materialComposeVersion = "1.1.0"
    viewModelcomposeVersion = "2.4.0"

    // Dagger Hilt
    daggerHiltVersion = "2.38.1"

    // Coroutines
    coroutinesVersion = "1.5.2"

    // Coil
    coilVersion = "1.4.0"

    // Testing Libraries
    junitVersion = "4.13.2"
    mockkVersion = "1.12.0"
    testJunitVersion = "1.1.3"
    espressoVersion = "3.4.0"
    coreTestingVersion = "2.1.0"
    extensionTestingVersion = "1.4.0"
    testMonitorVersion = "1.4.0"
}
